# Optional. Only needed to run StatsEngine as an MCP server (mcp_server/server.py).
# Core StatsEngine (core/, templates/) has no dependency on this file or on `mcp`.
#
# Install with:  pip install -r requirements.txt -r requirements-mcp.txt

mcp>=1.2.0
